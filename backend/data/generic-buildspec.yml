version: 0.2

phases:
  pre_build:
    commands:
      # ── 1. Validate required environment variables ──────────────────────────
      - |
        [ -z "$AWS_REGION" ]     && echo "ERROR: AWS_REGION is not set."     >&2 && exit 1 || true
        [ -z "$ECR_REPO_URI" ]   && echo "ERROR: ECR_REPO_URI is not set."   >&2 && exit 1 || true
        [ -z "$CONTAINER_NAME" ] && echo "ERROR: CONTAINER_NAME is not set." >&2 && exit 1 || true
        echo "All required environment variables are present."

      # ── 2. Authenticate to Amazon ECR ───────────────────────────────────────
      - echo "Logging in to Amazon ECR..."
      - aws ecr get-login-password --region "$AWS_REGION" | docker login --username AWS --password-stdin "$ECR_REPO_URI"

      # ── 3. Set image tag to commit SHA (immutable), fallback to 'latest' ────
      - IMAGE_TAG="${CODEBUILD_RESOLVED_SOURCE_VERSION:-latest}"
      - echo "Image tag -> $IMAGE_TAG"

      # ── 4. Pull latest for Docker layer cache (failure is OK if first run) ──
      - docker pull "$ECR_REPO_URI:latest" || true

  build:
    commands:
      # ── 5. Build Docker image with layer cache ───────────────────────────────
      - echo "Building Docker image..."
      - |
        docker build \
          --cache-from "$ECR_REPO_URI:latest" \
          --build-arg BUILDKIT_INLINE_CACHE=1 \
          -t "$ECR_REPO_URI:$IMAGE_TAG" \
          -t "$ECR_REPO_URI:latest" \
          .

  post_build:
    commands:
      # ── 6. Push both the commit-SHA tag and latest to ECR ───────────────────
      - echo "Pushing images to ECR..."
      - docker push "$ECR_REPO_URI:$IMAGE_TAG"
      - docker push "$ECR_REPO_URI:latest"

      # ── 7. Generate per-environment imagedefinitions JSON files ──────────────
      #
      # Each pipeline deploy stage reads its own file:
      #   Deploy-Dev  → imagedefinitions-dev.json
      #   Deploy-UAT  → imagedefinitions-uat.json
      #   Deploy-Prod → imagedefinitions-prod.json
      #
      # When SECRET_ARN_{ENV} is set, generates a FULL ECS task definition JSON
      # (with Secrets Manager references) so the pipeline's Deploy action
      # creates a new task def revision with all settings intact.
      #
      # SECURITY: Only SECRET_ARN_* (ARNs, not values) and SECRET_KEYS_*
      # (key names, not values) are used here. Actual secret values never
      # appear in this buildspec or CodeBuild logs.
      - |
        EXECUTION_ROLE="${EXECUTION_ROLE_ARN:-}"
        TASK_ROLE="${TASK_ROLE_ARN:-$EXECUTION_ROLE}"
        LOG_GROUP="${LOG_GROUP_NAME:-/ecs/$CONTAINER_NAME}"
        CONTAINER_PORT="${CONTAINER_PORT:-3000}"

        generate_taskdef() {
          local ENV_UPPER="$1"
          local SECRET_ARN_VAR="SECRET_ARN_${ENV_UPPER}"
          local SECRET_KEYS_VAR="SECRET_KEYS_${ENV_UPPER}"
          local OUTPUT_FILE="imagedefinitions-$(echo "$ENV_UPPER" | tr 'A-Z' 'a-z').json"
          local SECRET_ARN_VAL="${!SECRET_ARN_VAR:-}"
          local SECRET_KEYS_VAL="${!SECRET_KEYS_VAR:-}"

          if [ -n "$SECRET_ARN_VAL" ] && [ "$SECRET_ARN_VAL" != '""' ] && [ "$SECRET_ARN_VAL" != "" ]; then
            echo "Generating $OUTPUT_FILE with Secrets Manager references..."

            SECRETS_ARRAY=$(echo "$SECRET_KEYS_VAL" | node -e "
              const keys = JSON.parse(require('fs').readFileSync(0, 'utf8'));
              const arn = process.env.SECRET_ARN_VAL;
              console.log(JSON.stringify(keys.map(k => ({ name: k, valueFrom: arn + ':' + k + '::' }))));
            ")

            cat > "$OUTPUT_FILE" <<EOF_TASKDEF
          {
            "family": "$CONTAINER_NAME",
            "requiresCompatibilities": ["FARGATE"],
            "networkMode": "awsvpc",
            "cpu": "256",
            "memory": "512",
            "executionRoleArn": "$EXECUTION_ROLE",
            "taskRoleArn": "$TASK_ROLE",
            "containerDefinitions": [{
              "name": "$CONTAINER_NAME",
              "image": "$ECR_REPO_URI:$IMAGE_TAG",
              "essential": true,
              "portMappings": [{
                "containerPort": $CONTAINER_PORT,
                "protocol": "tcp"
              }],
              "logConfiguration": {
                "logDriver": "awslogs",
                "options": {
                  "awslogs-group": "$LOG_GROUP",
                  "awslogs-region": "$AWS_REGION",
                  "awslogs-stream-prefix": "$CONTAINER_NAME",
                  "awslogs-create-group": "true"
                }
              },
              "secrets": $SECRETS_ARRAY
            }]
          }
      EOF_TASKDEF
          else
            echo "Generating $OUTPUT_FILE (no secrets)..."
            printf '[{"name":"%s","imageUri":"%s"}]' "$CONTAINER_NAME" "$ECR_REPO_URI:$IMAGE_TAG" > "$OUTPUT_FILE"
          fi
          cat "$OUTPUT_FILE"
        }

        # Generate for all three environments
        generate_taskdef "DEV"
        generate_taskdef "UAT"
        generate_taskdef "PROD"

      # ── 8. Free disk space by removing local image layers ───────────────────
      - docker rmi "$ECR_REPO_URI:$IMAGE_TAG" "$ECR_REPO_URI:latest" || true

artifacts:
  files:
    - imagedefinitions-dev.json
    - imagedefinitions-uat.json
    - imagedefinitions-prod.json
  discard-paths: yes
